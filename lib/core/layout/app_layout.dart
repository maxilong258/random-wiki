abstract final class AppLayout {
  static const floatingBarHeight = 48.0;
  static const floatingBarOuterPadding = 14.0;
  static const backToTopBottom = floatingBarHeight + floatingBarOuterPadding + 30;
  static const listBottomReserve = floatingBarHeight + floatingBarOuterPadding + 58;
  static const pagedBottomReserve = floatingBarHeight + floatingBarOuterPadding + 40;
}
